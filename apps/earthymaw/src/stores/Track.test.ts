// import Track from "./Track";
// import * as Tone from 'tone';
// import MusicFeaturesStore from "stores/MusicFeatures.store";
// import TrackStore from "stores/Track.store";
// import RootStore from "stores/Root.store";


// let track: Track;
  
beforeEach(() => {
    // const audioContext = Tone.getContext();
    // const rootStore = new RootStore(audioContext);
    // const musicFeaturesStore = new MusicFeaturesStore(rootStore, audioContext);
    // const trackStore = new TrackStore(rootStore, audioContext);

    // tracqk = new Track(0, audioContext, musicFeaturesStore, trackStore);
});

test('track is created', () => {
    // expect(track).toBeInstanceOf(Track);
});