/* Importing the ParameterStore class from the Parameter.store.ts file. */
// import ParameterStore from "./Parameter.store";
// import RootStore from "./Root.store";
// import MembraneSynth from "./Synthesizer/SynthesizerTypes/MembraneSynth";

// import { AudioContext } from 'standardized-audio-context-mock';

// let parameterStore: ParameterStore;

beforeEach(() => {
    // const audioContext = new AudioContext();
    // const rootStore = new RootStore(audioContext, true);
    // parameterStore = rootStore.parameterStore;
});

test('Can get synthesizer parameters', () => {    // expect(track).toBeInstanceOf(Track);
    // const synth = new MembraneSynth({
    //     name: 'string',
    //     description: 'string',
    //     slug: 'string',
    //     tags: [''],
    //     type: 'MembraneSynth'
    // });
    // let parameters = parameterStore.fetchForSynth(synth, 1);
    // expect(parameters[0]!.name).toEqual("pitch");
});