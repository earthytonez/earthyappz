import { Grid } from "@mui/material";

import { observer } from "mobx-react-lite";

const Activity = observer((): React.ReactElement => {
  return <Grid></Grid>;
});

// Tree Planting Strategy (Dense/Sparse)
// Rest (At Home, in Place.
//   Resting at Home is more efficient.
//   Resting with fire is more efficient.

export default Activity;
